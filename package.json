{
  "name": "earnstayserver",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "install-chromium": "node install-chromium.js",
    "build": "npm install --legacy-peer-deps && npm run install-chromium"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "chrome-aws-lambda": "^10.1.0",
    "cors": "^2.8.5",
    "dns": "^0.2.2",
    "express": "^4.18.1",
    "puppeteer-core": "^10.1.0"
  }
}
